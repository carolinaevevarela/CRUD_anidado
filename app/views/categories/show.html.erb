<p id="notice"><%= notice %></p>

<h1>Categoría: <%= @category.name %> </h1>

<h2>Productos</h2>

<ul>
<% @product.each do |p| %>
  <li><%= p.name %> <%= link_to "x", category_product_path(@category, product),
    method: :delete%>
  </li>
  <li><%= p.price %> <%= link_to "x", category_product_path(@category, product),
    method: :delete%>
  </li>
<% end %>
</ul>

  <%= form_with(model: [@category, Product.new], local: true) do |f| %>
  <%= f.hidden_field :category_id, value: @category.id %>
    <div class="">
      <%= f.label :name, "Nombre" %>
      <%= f.text_field :name %>
    </div>
    <div class="">
      <%= f.label :price, "Precio" %>
      <%= f.number_field :price %>
    </div>
    <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
